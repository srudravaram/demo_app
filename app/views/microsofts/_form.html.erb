<%= form_for(@microsoft) do |f| %>
  <% if @microsoft.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@microsoft.errors.count, "error") %> prohibited this microsoft from being saved:</h2>

      <ul>
      <% @microsoft.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
